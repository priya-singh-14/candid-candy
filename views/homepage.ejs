<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Help - this isn't a homepage yet</h1>

    <% for(let i = 0; i < hearts.length; i++) { %>
      <div class="heart">
          <h2><%= hearts[i].date %></h2>
          <p>to: <%= hearts[i].to %></p>
          <p><%= hearts[i].text %></p>
          <p> ~ Anonymous %></p>
      </div>
    <% } %>
  </body>
</html>
